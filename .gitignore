node_modules
.env
.env.*
.DS_Store
*.log
dist/*
# The prebuilt browser ESM IS committed so jsdelivr can serve it from a tag to
# no-bundler apps (`<script type=module>` import). Rebuild with `bun run build:dist`.
!dist/app-shell.js
